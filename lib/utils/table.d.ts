import { EditorState } from 'prosemirror-state';
export declare function isTableActive(state: EditorState): boolean;
export declare function enableMergeCells(state: EditorState): boolean;
export declare function enableSplitCell(state: EditorState): boolean;
